apiVersion: v1
kind: Service
metadata:
  name: sidegit
spec:
  selector:
    app: sidegit
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
# To expose externally: change type to LoadBalancer, or front with an Ingress.
